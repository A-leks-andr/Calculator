[project]
name = "calculator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flet[all]>=0.86.3",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.21.0",
    "ruff>=0.16.0",
]
